---
import Layout from '../layouts/Layout.astro';
import TwoColumnLayout from '../layouts/TwoColumnLayout.astro';
import Cover from '../components/ui/cover/Cover.astro';
import HomeSider from '../components/layout/HomeSider.astro';
import QuoteMarquee from '../components/fun/QuoteMarquee.astro';
import { getSeoConfig, getUIText } from '../lib/i18n';
import { getCollection } from 'astro:content';
import { parseDate } from '../lib/datetime';
import { routeBuilder } from '../lib/route';
import { Routes } from '../constants/router';
import type { BlogPost } from '../types/blog';

const seoConfig = getSeoConfig('zh');
const uiText = getUIText('zh');

// 获取期权觉醒系列文章
const allPosts = await getCollection('blog');
const awakeningPosts = allPosts
  .filter(post => {
    const lang = post.data.lang || 'zh';
    const title = post.data.title || '';
    return lang === 'zh' && title.includes('期权觉醒篇');
  })
  .sort((a, b) => {
    // 按标题中的编号排序
    const numA = parseInt(a.data.title.match(/\d+/)?.[0] || '0');
    const numB = parseInt(b.data.title.match(/\d+/)?.[0] || '0');
    return numA - numB;
  });
---

<Layout title={`期权精品课 | ${seoConfig.title}`}>
  <TwoColumnLayout>
    <Cover slot="cover" title="期权精品课" />
    <HomeSider slot="sider" lang="zh" />
    <QuoteMarquee />
    
    <div class="shadow-box bg-gradient-start mx-0 flex w-full flex-col px-6 py-4 md:px-3 md:py-2">
      <!-- 课程介绍 -->
      <div class="mb-8 border-l-4 border-blue-500 bg-blue-50 dark:bg-blue-950/30 px-6 py-4 rounded-r-lg">
        <h2 class="text-2xl font-bold text-blue-600 dark:text-blue-400 mb-3">🔥 期权精品课</h2>
        <p class="text-muted-foreground leading-relaxed">
          精选期权交易核心课程，系统化学习期权卖方策略与现金流构建。从思维认知到实战技巧，助你建立完整的期权交易体系。
        </p>
      </div>

      <!-- 期权觉醒系列 -->
      <div class="mb-6">
        <h3 class="text-xl font-bold mb-4 flex items-center gap-2">
          <span class="inline-block w-1 h-6 bg-gradient-to-b from-blue-500 to-purple-500 rounded"></span>
          期权觉醒系列
        </h3>
        <div class="grid gap-4 md:grid-cols-1 sm:grid-cols-1">
          {awakeningPosts.map((post) => (
            <a 
              href={routeBuilder(Routes.Post, post as BlogPost, 'zh')}
              class="group block bg-card hover:bg-accent border border-border rounded-lg p-5 transition-all duration-300 hover:shadow-lg hover:scale-[1.02]"
            >
              <div class="flex items-start gap-4">
                <div class="flex-shrink-0 w-12 h-12 bg-gradient-to-br from-blue-500 to-purple-500 rounded-lg flex items-center justify-center text-white font-bold text-lg">
                  {post.data.title.match(/\d+/)?.[0]}
                </div>
                <div class="flex-1">
                  <h4 class="text-lg font-semibold text-primary group-hover:text-blue-600 dark:group-hover:text-blue-400 mb-2 transition-colors">
                    {post.data.title}
                  </h4>
                  <p class="text-sm text-muted-foreground line-clamp-2 mb-2">
                    {post.data.description || '期权交易核心课程'}
                  </p>
                  <div class="flex items-center gap-4 text-xs text-muted-foreground">
                    <span>📅 {parseDate(post.data.date, 'YYYY-MM-DD')}</span>
                    {post.data.tags && post.data.tags.length > 0 && (
                      <div class="flex gap-1">
                        {post.data.tags.slice(0, 3).map((tag: string) => (
                          <span class="px-2 py-0.5 bg-primary/10 rounded-full">#{tag}</span>
                        ))}
                      </div>
                    )}
                  </div>
                </div>
              </div>
            </a>
          ))}
        </div>
      </div>

      <!-- 课程特色 -->
      <div class="mt-8 grid grid-cols-1 md:grid-cols-3 gap-4">
        <div class="bg-gradient-to-br from-blue-50 to-blue-100 dark:from-blue-950/30 dark:to-blue-900/20 rounded-lg p-4">
          <div class="text-3xl mb-2">🎯</div>
          <h4 class="font-semibold mb-1 text-blue-900 dark:text-blue-300">系统化学习</h4>
          <p class="text-sm text-muted-foreground">从基础到进阶，构建完整知识体系</p>
        </div>
        <div class="bg-gradient-to-br from-purple-50 to-purple-100 dark:from-purple-950/30 dark:to-purple-900/20 rounded-lg p-4">
          <div class="text-3xl mb-2">💰</div>
          <h4 class="font-semibold mb-1 text-purple-900 dark:text-purple-300">实战导向</h4>
          <p class="text-sm text-muted-foreground">真实案例，可落地的交易策略</p>
        </div>
        <div class="bg-gradient-to-br from-green-50 to-green-100 dark:from-green-950/30 dark:to-green-900/20 rounded-lg p-4">
          <div class="text-3xl mb-2">🛡️</div>
          <h4 class="font-semibold mb-1 text-green-900 dark:text-green-300">风控优先</h4>
          <p class="text-sm text-muted-foreground">安全第一，稳健盈利</p>
        </div>
      </div>
    </div>
  </TwoColumnLayout>
</Layout>
