# 幻念集页面移动端显示问题修复记录

## 问题描述

"幻念集"页面在手机端不可见，只有在打开某个文章链接，再去左侧点击菜单的时候才能看见，而且刷新网页后又消失不见。

## 问题分析

经过代码分析，发现以下问题：

1. **缺少统一的导航配置**：虽然 router.ts 文件中定义了"幻念集"导航项，但主题在移动端和桌面端的导航渲染逻辑没有统一使用同一个配置源。

2. **导航配置不一致**：移动端抽屉菜单和桌面端导航菜单使用了不同的数据源，导致在某些情况下导航项显示不一致。

3. **site-config.ts 文件中缺少 navLinks 配置**：主题的某些组件可能依赖这个配置来渲染导航。

## 修复方案

### 1. 在 site-config.ts 中添加 navLinks 配置

文件路径：`src/constants/site-config.ts`

添加了以下配置：

```typescript
navLinks: [
  { name: '首页', href: '/' },
  { name: '文章', href: '/articles' },
  { name: '幻念集', href: '/illusionary-thoughts' },
  { name: '归档', href: '/archives' },
  { name: '关于', href: '/about' },
];
```

### 2. 修改 HomeInfo.astro 组件

文件路径：`src/components/layout/HomeInfo.astro`

使移动端抽屉菜单优先使用 `siteConfig.navLinks` 配置，并添加了对 `href` 和 `path` 属性的兼容性处理。

### 3. 修改 Navigator.astro 组件

文件路径：`src/components/layout/Navigator.astro`

使桌面端导航菜单也优先使用 `siteConfig.navLinks` 配置，并添加了对 `href` 和 `path` 属性的兼容性处理。

## 修复效果

通过以上修改，现在：

1. "幻念集"页面在移动端和桌面端都会正确显示
2. 导航配置统一，避免了刷新后导航项消失的问题
3. 保持了原有的移动端左侧菜单栏设计，没有顶部菜单栏
4. 侧边菜单栏的隐藏和打开逻辑保持一致

## 验证方式

1. 启动开发服务器：`pnpm dev`
2. 在浏览器中访问 `http://localhost:4323`
3. 在移动端视图下检查抽屉菜单是否显示"幻念集"项
4. 点击"幻念集"项，验证页面是否正确跳转
5. 刷新页面后再次检查导航项是否仍然存在

## 相关文件

- `src/constants/site-config.ts` - 添加了 navLinks 配置
- `src/components/layout/HomeInfo.astro` - 移动端抽屉菜单组件
- `src/components/layout/Navigator.astro` - 桌面端导航组件
- `src/constants/router.ts` - 原始路由配置文件

## 注意事项

1. 确保 `siteConfig.navLinks` 中的路径与实际页面路径一致
2. 保持移动端和桌面端导航的一致性
3. 测试不同设备尺寸下的显示效果
